<tr>
<% list(options.columns, function(col) { %>
  <% if(col.attr('visible') !== false) { %>
    <th <%=  (el) -> can.data( el, 'column', col) %>>
      <%= col.attr('text') %>
    </th>
  <% } %>
<% }) %>
</tr>
